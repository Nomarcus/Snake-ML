name: Deploy Snake-ML to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare static site bundle
        run: |
          rm -rf dist
          mkdir -p dist
          cp index.html dist/
          if [ -f hf-tuner.js ]; then cp hf-tuner.js dist/; fi
          if [ -f __hf_key.js ]; then cp __hf_key.js dist/; fi
          if [ -f .nojekyll ]; then cp .nojekyll dist/; fi
          if [ -d src ]; then cp -R src dist/src; fi
          if [ -d public ]; then cp -R public/. dist/; fi

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v3
